# Финальные правки сайта АВИАТОР

Внесено в финальную статическую версию:

1. Заголовок «Профессиональный симулятор Airbus A320» разбит на три строки и больше не обрезается.
2. Логотип в шапке смещён правее; слово «АВИАТОР» отделено от линии знака, наложения нет.
3. Убраны технические маркировки разделов вроде `01 / PROBLEM`, `03 / FOCUS` и аналогичные подписи.
4. Карточка Артура настроена через `object-fit: contain`, чтобы портрет не обрезался внутри блока.
5. Первый экран очищен: фон затемнён на текстовой зоне, сетка оставлена тонкой, изображение не мешает чтению.
6. Добавлены контакты из презентации: `+7 926 537-41-18`, `departure.aviator@gmail.com`, `@aviator`.
7. Добавлены CTA-кнопки через `mailto:` и `tel:` для временного запуска без backend.
8. Подготовлена структура под GitHub Pages: `CNAME`, `.nojekyll`, `robots.txt`, `sitemap.xml`.
